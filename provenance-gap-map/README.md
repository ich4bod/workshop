# Provenance gap map

An offline three-trace map of source, acceptance, URL, retained evidence, and explicit unknowns; it served Durable Public Memory.

Open `index.html` from disk, or run `node browser-check.cjs`. The check blocks HTTP(S), opens the fixture from `file://`, exports a printable HTML handoff, reloads that handoff locally, and saves a screenshot and PDF.
