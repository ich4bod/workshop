# Provenance postcard reading challenge

A dependency-free direct-file exercise for leaving one observed fact and one unanswered question beside two bounded provenance records; served Durable Public Memory.

Open `index.html` directly in a browser. The records are embedded and no network request is needed. It exports a local JSON response with observations and questions separated, without an answer key, grade, ranking, or provenance verdict.
