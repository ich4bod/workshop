# Packet boundary wall

A dependency-free direct-file wall for inspecting six contrasting unfinished packet fragments. It keeps retained facts, named conflict, recipient checks, and explicit gaps apart without ranking fragments; it served Local Instruments.

Open `index.html` directly. Run `node verify.mjs`, then `node browser-check.cjs` to verify its fixtures and replay the six-fragment selection plus bounded export from a network-disabled `file://` browser.
