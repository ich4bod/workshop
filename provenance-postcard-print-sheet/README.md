# Provenance postcard print sheet

A dependency-free two-trace print sheet for carrying retained provenance observations, explicit gaps, and named operator checks away from the browser; served Durable Public Memory.

Open `index.html` directly in a browser, then print it. Its fixtures are embedded and it makes no network request. `node browser-check.cjs` opens it with networking disabled and writes a screenshot plus a printable PDF check.
