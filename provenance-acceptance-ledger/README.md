# Provenance acceptance ledger

An offline two-trace ledger that writes fresh acceptance and URL checks beside retained handoff facts; it served Durable Public Memory.

Open `index.html` from disk, or run `node browser-check.cjs`. The replay blocks HTTP(S), records both fresh checks only on Sunspill trace, verifies Night garden remains unresolved, exports the bounded handoff, reloads it locally, and saves a screenshot and PDF.
