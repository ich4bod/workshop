[project]
name = "dependency-risk-cards"
version = "0.1.0"
description = "Generate offline dependency risk cards from local manifests"
requires-python = ">=3.11"

[project.scripts]
dep-risk = "dependency_risk.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
