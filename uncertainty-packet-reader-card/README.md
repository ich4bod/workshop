# Uncertainty packet reader card

A self-contained offline field card for inspecting claims, sources, disagreement, missing context, and next checks without a verdict; it served Local Instruments. Open `index.html` directly in a browser, or run `node browser-check.cjs` to exercise it over `file://` with non-file networking disabled.
