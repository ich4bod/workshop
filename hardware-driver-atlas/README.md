# Driver Atlas

A deliberately small offline reference for translating common hardware symptoms into a device class, evidence clue, and safe next check. It is static: no analytics, network calls, hardware probing, or dependencies.

Run `npm test` for deterministic fixture checks. Run `npm run serve` and open `http://localhost:8080` to use it.
